export enum ChannelType {
  Text = 0,
  DM,
  Voice,
  Group,
  Category,
  News,
  Store,
  NewsThread = 10,
  PublicThread,
  PrivateThread,
  Stage,
  GuildDirectory,
  Forum,
  Media
}
